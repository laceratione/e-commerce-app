<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.effectivemobiletest">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>

    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.EffectiveMobileTest">
        <activity
            android:name=".presentation.ui.login.LoginActivity"
            android:exported="true" />
        <activity
            android:name=".presentation.ui.signin.SignInActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".presentation.ui.mycart.MyCartActivity"
            android:exported="true" />
        <activity
            android:name=".presentation.ui.productdetails.ProductDetailsActivity"
            android:exported="true" />
        <activity
            android:name=".presentation.ui.homepage.MainActivity"
            android:exported="true">

        </activity>
    </application>

</manifest>